<div class="form-group">
    <label for="{{form_id}}_{{name}}">
        {{display_name}}:
        <textarea id="{{form_id}}_{{name}}" name="{{form_id}}_{{name}}">{{value}}</textarea>
    </label>
</div>
